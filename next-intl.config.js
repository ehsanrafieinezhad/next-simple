const config = {
    locales: ['en', 'de'],
    defaultLocale: 'en',
    // Optional: Specify additional locale settings
    localeDetection: false // Disable automatic locale detection
  };
  
  module.exports = config;